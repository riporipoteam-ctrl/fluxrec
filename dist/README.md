# Built Flux Rec package

- `RecNetPlugin.dll` — built from `plugin/`.
- `PreloaderPatcher.dll` — built from `preloader/`.
- `FluxRec-runtime.zip` — project-owned server, scripts, configuration, documentation, licenses, and the two compiled DLLs.

No Rec Room or third-party runtime binaries are included.

