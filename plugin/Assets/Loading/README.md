# Optional loading artwork

Place an authorized custom image at `Activity_Image_Orientation.png` to embed it into the plugin. That filename is ignored by Git because the development copy originated from proprietary game artwork.

When the file is absent, the plugin looks for `Activity_Image_Orientation` in the locally installed game's Unity resources and then uses the game's default loading picture.

